var html = `<%= render 'comments/comment', comments: @comments %>`;
var commentArea = document.getElementById("comments");

commentArea.innerHTML = "";
commentArea.insertAdjacentHTML('beforeend', html);


